class Todo {

    constructor(title, dueDate, priority, status) {
        this.title = title;
        this.dueDate = dueDate;
        this.priority = priority;
        this.status = status;
    };

};

export default Todo;