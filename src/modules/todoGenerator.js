class Todo {

    constructor(id, title, branch, priority, status) {
        this.id = id;
        this.title = title;
        this.branch = branch;
        this.priority = priority;
        this.status = status;
    };

};

export default Todo;