class Todo {

    constructor(title, branch, priority, status) {
        this.title = title;
        this.branch = branch;
        this.priority = priority;
        this.status = status;
    };

};

export default Todo;