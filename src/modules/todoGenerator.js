class Todo {
  constructor (title, branch, status, priority, date) {
    this.title = title
    this.branch = branch
    this.status = status
    this.priority = priority
    this.date = date
  };
};

export default Todo
