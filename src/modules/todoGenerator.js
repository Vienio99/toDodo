class Todo {

    constructor(title, branch, dueDate, priority, status) {
        this.title = title;
        this.branch = branch;
        this.dueDate = dueDate;
        this.priority = priority;
        this.status = status;
    };

};

export default Todo;